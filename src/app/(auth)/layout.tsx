// Este layout de autenticação foi removido como parte do reset para uma interface simples.
// O conteúdo pode ser recriado se a autenticação for adicionada novamente.
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
