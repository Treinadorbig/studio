import {
  Dumbbell, // Using as Logo for now
  Activity,
  type LucideProps,
} from 'lucide-react';

export const Icons = {
  Logo: (props: LucideProps) => <Dumbbell {...props} />,
  Activity, // Example icon
};

export default Icons;
